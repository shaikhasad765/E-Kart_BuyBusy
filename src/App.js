// Importing the "Layout" component from the "./Components/Layout/Layout" file
import Layout from "./Components/Layout/Layout";

// Importing the "App.css" file to apply styles to the app
import "./App.css"

// The main component "App" that renders the application
const App = () => {
  // Rendering the "Layout" component
  return (
    <Layout/>
  );
};

// Exporting the "App" component as the default export
export default App;
